import { SpanishTranslationPipe } from './spanish-translation.pipe';

describe('SpanishTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new SpanishTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
